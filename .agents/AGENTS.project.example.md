# AGENTS.md

Project-specific instructions for this repository. Keep this file short: include
only details that a coding agent cannot reliably infer from the code, README, or
standard tooling.

## Purpose

- Describe what this repository does in one or two sentences.
- Mention the primary runtime, framework, or package manager only when it is not
  obvious from standard files.

## Commands

- Install dependencies: `TODO`
- Run the app or service: `TODO`
- Run focused tests: `TODO`
- Run lint/type checks: `TODO`
- Run the full validation suite: `TODO`

## Project Structure

- `TODO/`: explain non-obvious ownership or responsibilities.
- `TODO/`: mention generated files, fixtures, or directories agents should avoid.

## Conventions

- Include only project-specific conventions that differ from common defaults.
- Prefer examples that name exact files, commands, or APIs.
- Do not restate formatter or linter rules that deterministic tools already enforce.

## Verification

- State what must be true before work is done.
- Prefer the smallest relevant check for ordinary edits.
- Name any expensive or flaky checks so the agent knows when to ask first.

## Boundaries

- Ask before changing public APIs, data models, migrations, CI, release scripts,
  or security-sensitive configuration.
- Never commit secrets, tokens, credentials, or machine-specific values.
- List files, directories, or generated artifacts that should not be edited.

## Notes for Agents

- Read referenced docs only when they are relevant to the current task.
- If instructions appear stale or conflict with the code, stop and report the mismatch instead of guessing.
