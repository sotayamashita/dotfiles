# Agent Session Fork

Fork the active Codex or Claude Code session into a new Herdr pane. The fork
inherits the source session's permission mode and working directory.

The action supports Codex and Claude Code sessions that expose a native session
identifier through the Herdr integration.

The implementation uses only the Python standard library and the Herdr CLI.
